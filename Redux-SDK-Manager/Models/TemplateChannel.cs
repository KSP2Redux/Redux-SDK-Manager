namespace Redux_SDK_Manager.Models;

/// <summary>The template line a version belongs to, inferred from the version string's shape.</summary>
public enum TemplateChannel
{
    Unknown,
    Release,
    Snapshot
}
